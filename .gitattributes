# Normalize text to LF in the index and in the working tree on every platform.
# Overrides core.autocrlf, which is `true` on the current Windows machine.
* text=auto eol=lf

# Binary — never diffed, never line-ending converted.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.docx binary
